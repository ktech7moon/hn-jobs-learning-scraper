You are polishing the header of a Markdown digest produced by an AI agent that scraped Hacker News "Who's Hiring" for new postings. The digest is the first thing a portfolio reviewer sees — it has to read like a senior engineer wrote it, not like an autogenerated report.

# Your job

Rewrite ONLY the header — the lines from the H1 title down to (but not including) the first `## ` section. Keep:

- The H1 title verbatim.
- Every cited number (jobs, cost, wall-clock, calls, candidate comments) — they appear in `FACTS:` exactly as the user gave them.
- The links (source URL).

Tighten the prose. Cut throat-clearing. The reader is a hiring manager who wants the headline numbers in 5 seconds.

# Constraints

- Output ONLY the rewritten header. No preamble, no postamble, no explanation, no code fences.
- Do NOT invent or change numbers. Every dollar amount, count, and duration must match `FACTS:` to the digit.
- Do NOT introduce comparisons against any baseline that is not present in `FACTS:`. If a `naive_baseline_*` key appears in `FACTS:`, you may cite it; if it does not, do not invent one. (The naive baseline only exists for thread 47975571 from Slice 1; April 2026 and other threads have no comparable run.)
- Do NOT add subjective puff ("blazingly fast", "elegantly designed"). State numbers plainly.
- Format: Markdown. Bold field labels (`**Wall-clock:**`, etc.) are fine. Keep ≤12 lines.
- Keep the parenthetical pricing footnote (the line about token counts × Anthropic public list pricing). Don't drop it.

# Output

Return the polished header markdown. Stop at the line just before the first `## ` section break — the harness will splice your output back in.
